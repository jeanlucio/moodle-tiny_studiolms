# See https://github.com/moodlehq/moodle-plugin-release
#
# Triggered automatically after "Moodle Plugin CI" completes successfully
# on a tag push, or manually via workflow_dispatch.
#
# Required secret: MOODLE_ORG_TOKEN
# (generate at https://moodle.org/user/profile.php → Security keys)
#
name: Releasing in the Plugins directory

on:
  workflow_run:
    workflows: ["Moodle Plugin CI"]
    types: [completed]
    branches:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to be released'
        required: true

defaults:
  run:
    shell: bash

jobs:
  release-at-moodle-org:
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'push' &&
       startsWith(github.event.workflow_run.head_branch, 'v'))
    env:
      PLUGIN: tiny_studiolms
      CURL: curl -s
      ENDPOINT: https://moodle.org/webservice/rest/server.php
      TOKEN: ${{ secrets.MOODLE_ORG_TOKEN }}
      FUNCTION: local_plugins_add_version

    steps:
      - name: Determine tag name
        id: get-tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [[ -n "${{ github.event.inputs.tag }}" ]]; then
            TAGNAME="${{ github.event.inputs.tag }}"
          else
            TAGNAME=$(gh api "repos/${{ github.repository }}/git/refs/tags" \
              --jq '.[] | select(.object.sha == "${{ github.event.workflow_run.head_sha }}") | .ref | ltrimstr("refs/tags/")')
          fi
          if [[ -z "${TAGNAME}" ]]; then
            echo "No tag found for this run — skipping release."
            exit 0
          fi
          echo "tagname=${TAGNAME}" >> $GITHUB_OUTPUT

      - name: Call the service function
        id: add-version
        if: ${{ steps.get-tag.outputs.tagname != '' }}
        run: |
          TAGNAME="${{ steps.get-tag.outputs.tagname }}"
          ZIPURL="https://api.github.com/repos/${{ github.repository }}/zipball/${TAGNAME}"
          RESPONSE=$(${CURL} ${ENDPOINT} \
            --data-urlencode "wstoken=${TOKEN}" \
            --data-urlencode "wsfunction=${FUNCTION}" \
            --data-urlencode "moodlewsrestformat=json" \
            --data-urlencode "frankenstyle=${PLUGIN}" \
            --data-urlencode "zipurl=${ZIPURL}" \
            --data-urlencode "vcssystem=git" \
            --data-urlencode "vcsrepositoryurl=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}" \
            --data-urlencode "vcstag=${TAGNAME}" \
            --data-urlencode "changelogurl=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/commits/${TAGNAME}" \
            --data-urlencode "altdownloadurl=${ZIPURL}")
          echo "response=${RESPONSE}" >> $GITHUB_OUTPUT

      - name: Evaluate the response
        id: evaluate-response
        if: ${{ steps.get-tag.outputs.tagname != '' }}
        env:
          RESPONSE: ${{ steps.add-version.outputs.response }}
        run: |
          jq <<< ${RESPONSE}
          jq --exit-status ".id" <<< ${RESPONSE} > /dev/null
