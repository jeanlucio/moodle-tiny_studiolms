define("tiny_studiolms/frontend",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * Student-facing frontend engine for StudioLMS blocks.
   *
   * Loaded on course/module pages. Handles runtime block behaviours
   * (accordion toggle, sounds, hover effects) that need JavaScript.
   *
   * Templates use <div> instead of <details>/<summary> because Moodle's
   * HTMLPurifier (XHTML 1.0 Transitional) strips those HTML5 elements.
   * State is encoded via the .slms-closed CSS class; sounds via .slms-snd-*;
   * hover effects via .slms-hov-* — all class-based to survive purification.
   *
   * @module     tiny_studiolms/frontend
   * @copyright  2026 Jean Lúcio <jeanlucio@gmail.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const SELECTORS_ACCORDION_SUMMARY=".studiolms-accordion-summary",SELECTORS_WEBTECA_SUMMARY=".studiolms-webteca-summary",SELECTORS_ANY_BLOCK=".studiolms-accordion, .studiolms-webteca, .studiolms-card, .studiolms-callout-wrap",playSound=type=>{if(type&&"none"!==type)try{const ctx=new(window.AudioContext||window.webkitAudioContext);if("click"===type){const bufLen=Math.floor(.06*ctx.sampleRate),buf=ctx.createBuffer(1,bufLen,ctx.sampleRate),data=buf.getChannelData(0);for(let i=0;i<bufLen;i++)data[i]=(2*Math.random()-1)*Math.exp(-i/(.25*bufLen));const src=ctx.createBufferSource();src.buffer=buf;const filter=ctx.createBiquadFilter();filter.type="lowpass",filter.frequency.value=900;const gain=ctx.createGain();gain.gain.value=.35,src.connect(filter),filter.connect(gain),gain.connect(ctx.destination),src.start()}else if("chime"===type){const osc=ctx.createOscillator(),gain=ctx.createGain();osc.type="sine",osc.frequency.value=880,gain.gain.setValueAtTime(.4,ctx.currentTime),gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.5),osc.connect(gain),gain.connect(ctx.destination),osc.start(),osc.stop(ctx.currentTime+.5)}else if("pop"===type){const osc=ctx.createOscillator(),gain=ctx.createGain();osc.type="sine",osc.frequency.setValueAtTime(220,ctx.currentTime),osc.frequency.exponentialRampToValueAtTime(55,ctx.currentTime+.09),gain.gain.setValueAtTime(.45,ctx.currentTime),gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.1),osc.connect(gain),gain.connect(ctx.destination),osc.start(),osc.stop(ctx.currentTime+.1)}}catch(e){}},getSoundClass=el=>{const match=[...el.classList].find((c=>c.startsWith("slms-snd-")));return match?match.replace("slms-snd-",""):"none"};_exports.init=()=>{document.querySelector(SELECTORS_ANY_BLOCK)&&((()=>{const summarySelector="".concat(SELECTORS_ACCORDION_SUMMARY,", ").concat(SELECTORS_WEBTECA_SUMMARY);document.querySelectorAll(summarySelector).forEach((summary=>{const container=summary.closest(".studiolms-accordion, .studiolms-webteca");if(!container)return;summary.setAttribute("role","button"),summary.setAttribute("tabindex","0"),summary.setAttribute("aria-expanded",container.classList.contains("slms-closed")?"false":"true");const toggle=()=>{const wasClosed=container.classList.contains("slms-closed");container.classList.toggle("slms-closed"),summary.setAttribute("aria-expanded",wasClosed?"true":"false"),wasClosed&&playSound(getSoundClass(container))};summary.addEventListener("click",toggle),summary.addEventListener("keydown",(e=>{"Enter"!==e.key&&" "!==e.key||(e.preventDefault(),toggle())}))}))})(),document.querySelectorAll("a.studiolms-btn[contenteditable]").forEach((btn=>{btn.removeAttribute("contenteditable")})),document.querySelectorAll("a.studiolms-btn").forEach((el=>{const sound=getSoundClass(el);sound&&"none"!==sound&&el.addEventListener("click",(()=>playSound(sound)))})))}}));

//# sourceMappingURL=frontend.min.js.map