{{!
    @template tiny_studiolms/toolbar_gauge

    Toolbar for the StudioLMS gauge block — shows the Edit button.

    Example context (json):
    {}
}}
<div class="slms-toolbar-seg">
    <button type="button" class="slms-toolbar-btn" id="tb-gauge-edit">
        <i class="fa-solid fa-gauge" aria-hidden="true"></i>
        {{#str}} toolbar_gauge_edit, tiny_studiolms {{/str}}
    </button>
</div>
