{{!
    @template tiny_studiolms/toolbar_mindmap

    Toolbar for the StudioLMS mind map block — shows the Edit button.

    Example context (json):
    {}
}}
<div class="slms-toolbar-seg">
    <button type="button" class="slms-toolbar-btn" id="tb-mindmap-edit">
        <i class="fa-solid fa-sitemap" aria-hidden="true"></i>
        {{#str}} toolbar_mindmap_edit, tiny_studiolms {{/str}}
    </button>
</div>
